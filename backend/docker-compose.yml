services:
  postgres:
    image: postgres:15-alpine
    container_name: animus_postgres
    environment:
      POSTGRES_USER: animus_user
      POSTGRES_PASSWORD: animus_password
      POSTGRES_DB: animus_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_animus:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U animus_user -d animus_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data_animus:
